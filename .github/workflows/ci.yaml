---
name: ci

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: No-op check
        run: echo "CI is wired up"
